#!/usr/bin/env zsh
hint=$1
default=$2
result=""
if command -v gopass &> /dev/null; then
    data=$(gopass find "chezmoi/$1" 2>/dev/null | head -n 1)
    result=$(gopass show --password $data 2> /dev/null)
fi
if [ -z "$result" ]; then
    printf "$2"
else
    printf "$result"
fi
